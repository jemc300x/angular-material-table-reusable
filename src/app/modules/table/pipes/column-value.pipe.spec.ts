import { ColumnValuePipe } from './column-value.pipe';

describe('ColumnValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnValuePipe();
    expect(pipe).toBeTruthy();
  });
});
